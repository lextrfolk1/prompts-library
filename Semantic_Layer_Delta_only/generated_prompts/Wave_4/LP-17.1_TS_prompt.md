# WORK PROMPT — LP-17.1 · React / TypeScript · shared
(parent LP-17 — Register Object wizard (5-step) · layer=shared)
(generated from Development Manifest 2.11.0+fable.1 (DELTA from 2.3.0) · Fable delta build)

## Role
You are the developer's AI copilot for ONE sub-task. First discover what already exists in the live
repo, decide REUSE/ADAPT/BUILD, then deliver code the developer reviews, owns, and commits.
Do not exceed scope. Do not alter any fixed contract.

## Exposed codebase (this session)
- Repository: semantic-service, database-migration-platform, frontend-service
- Primary Target: frontend-service (React 18 / TypeScript / MUI / AG Grid)
- Branch: semantic-feature-delta
- All reads and writes stay within this branch's structure and conventions.
- Repository topology is OPEN (monorepo vs multi-repo vs shared package) — do not assume a layout; flag layout-dependent choices.

## SHARED-LAYER GOVERNANCE (this is a shared component — tighter rules)
- Owner: Lextr Core. Consumers: Lextr Core, Lextr Intelligence, Lextr EUC Remediation.
- Cross-product regression is expected: validate the change against all consuming products.
- Independent deployability is the test: the change must not break any product's standalone deploy or couple products together.
- A change must state cross-product impact; if reusing, confirm the existing asset's contract is the one ALL products rely on.

## STEP 1 — Discover before you write (NON-WAIVABLE, do this first)
Search the exposed branch BEFORE writing any code. Report these sections, then proceed:
- SEARCHED: the exact terms and paths you searched across the exposed repositories.
- FOUND: each existing file/class/method/component that overlaps this sub-task, and what it does.
- DECISION: REUSE (extend/call it) | ADAPT (minimal change to an adjacent asset) | BUILD (nothing fits — say why).
- CROSS-PRODUCT: which of Lextr Core, Lextr Intelligence, Lextr EUC Remediation consume the asset, and whether your change preserves each product's independent deployability. Flag any coupling or consumer break.
Rules: if an existing controller/service/DAO/method/component/policy/migration covers this, EXTEND or CALL it —
never author a parallel one; never create a second class/endpoint/table/column/component that overlaps an existing one.
Honor low-code order: reuse > configuration > extension > new.

## PRIOR CORRECTIONS — apply these; do not repeat the corrected mistakes
- [IMPLEMENTATION] (this sub-task) Register Object selects the object from a governed inventory picker (Schema → Object Type → Object drill-down; registration-actionable subset default with all-objects toggle; not-eligible disabled+reason; 'Has updates' derived) instead of a free-text object code. — UI updated to prevent free-text/ungoverned object codes and to match the logical drill-down; the picker is fed by existing LP-05/LP-02 reads, so there is no schema or API contract change.
- [IMPLEMENTATION] (this sub-task) Replace free-text object-code input with a governed ObjectPicker; reorder to Schema -> Object Type -> Object; default the Register list to the registration-actionable queue with an All-objects toggle; add a 'Has Updates' derived status. — UI was brought forward to a governed inventory picker; the manifest LP-17 scope still described the free-text era. Picker is fed by existing reads, so this is FE/UX only with no contract impact.
Carry these forward. CONTRACT/STANDARD/DECISION items are owner-approved proposals where marked; do not act beyond what was approved.

## Parent logic point
- LP-17 — Register Object wizard (5-step) · FE · TS · layer=shared
- Wave Wave 4 · prerequisites: LP-03, LP-05, LP-15, LP-30

## Background (decided — do not re-open)
[AMENDED from v2.3.0 · REWORK] THIS LOGIC POINT IS ALREADY IMPLEMENTED.
You are MODIFYING existing code, not building new. Read the change list below before touching anything.
REWORK: something was REPLACED — behaviour that already shipped must change. Do not treat the removals as optional.

REMOVED from the v2.3.0 contract — the existing behaviour must be retired or replaced:
  - [scope] MUI Stepper: connection/object -> introspect -> names & roles (<=32 validation) -> taxonomy/CDE -> review & submit;
  - [background] A steward registers an object through a 5-step wizard, including taxonomy/MDRM capture with jurisdiction.
  - [background] Submit lands the object DRAFT for governance approval.
  - [manifest_of_steps] Stepper scaffold
  - [manifest_of_steps] Per-step validation
  - [manifest_of_steps] Taxonomy capture UI
  - [manifest_of_steps] Submit to LP-03
  - [tests_must_cover] 5 steps gate correctly
  - [tests_must_cover] logical_short_nm>32 blocks advance
  - [tests_must_cover] taxonomy fields captured + jurisdiction shown
  - [tests_must_cover] submit posts and lands DRAFT
  - [pre_build_knowledge] Behavior reference: prototype Register screen

ADDED in v2.11.0 — implement these on top of what exists:
  + [scope] MUI Stepper: schema -> object type -> object (governed inventory picker;
  + [scope] registration-actionable subset;
  + [scope] Not-Eligible disabled with reason) -> introspect -> names & roles (<=32 validation) -> taxonomy/CDE -> classification -> review & submit;
  + [scope] Register list = registration queue (default) with an All-objects toggle;
  + [scope] 'Has Updates' is a derived status (REGISTERED with attrReg<attrTotal).
  + [scope] GLOBAL tenant-scope indicator + object history drawer (LP-30).
  + [scope] Picker is fed by existing LP-05/LP-02 reads (object inventory + attribute counts);
  + [scope] 'registration-actionable' and 'Has Updates' are UI-derived — FE/UX only, no schema/contract change (additive, downstream-only).
  + [scope] The attribute step captures the full authoring contract (semantic/AI exposure per attribute, semantic role, CDE/DE + source, domain mode incl.
  + [scope] Enumerated-list inline values and Reference-domain/Reference-table link with code/desc column mapping, operational block, effective dates) and posts it to LP-03;
  + [scope] DOMAIN_INLINE_MAX_CARDINALITY guards inline vs reference.
  + [background] A steward registers an object through a 5-step wizard.
  + [background] The object is chosen from a GOVERNED INVENTORY PICKER (no free-text object code): a Schema -> Object Type -> Object drill-down filtered to the registration-actionable subset (New / Has-Updates / Pending / Not-Eligible).
  + [background] The Register list defaults to a registration queue (actionable subset) with an 'All objects' toggle, synced to the full object inventory.
  + [background] Taxonomy/MDRM with jurisdiction is captured;
  + [background] submit lands the object DRAFT for governance approval.
  + [manifest_of_steps] Governed ObjectPicker (Schema -> Object Type -> Object) sourced from LP-05/LP-02 reads
  + [manifest_of_steps] Registration-queue default view + All-objects toggle (synced to object inventory)
  + [manifest_of_steps] Not-Eligible disabled with reason; 'Has Updates' derived status
  + [manifest_of_steps] Stepper scaffold + per-step validation (<=32 names)
  + [manifest_of_steps] Taxonomy capture + classification step (LP-31 scheme)
  + [manifest_of_steps] Submit to LP-03; GLOBAL scope indicator + object history drawer (LP-30)
  + [tests_must_cover] object chosen from picker (no free-text object code)
  + [tests_must_cover] Schema -> Object Type -> Object drill-down filters the picker; changing schema/type clears object
  + [tests_must_cover] Not-Eligible options shown disabled with reason
  + [tests_must_cover] registration queue is default; All-objects toggle reveals full inventory
  + [tests_must_cover] 'Has Updates' derived for REGISTERED objects with attrReg<attrTotal
  + [tests_must_cover] 5 steps gate correctly; logical_short_nm>32 blocks advance
  + [tests_must_cover] submit posts and lands DRAFT; classification captured + validated
  + [tests_must_cover] Enumerated-list captures inline values; Reference-domain captures domain_cd or ref-table+code/desc cols
  + [tests_must_cover] inline domain blocked above DOMAIN_INLINE_MAX_CARDINALITY (policy_preset)
  + [pre_build_knowledge] Behavior reference: prototype Register screen + current Semantic Layer UI (Register Object)
  + [pre_build_knowledge] Object is chosen from a governed inventory picker, NOT a free-text object code [UPDATE v2.7.0]
  + [pre_build_knowledge] Field order is the logical drill-down Schema → Object Type → Object [UPDATE v2.7.0]
  + [pre_build_knowledge] Picker is sourced from existing reads (LP-05 object inventory incl. attribute counts; LP-02 schemas); 'registration-actionable' and 'Has updates' are UI-derived [UPDATE v2.7.0]
  + [pre_build_knowledge] FE/UX refinement only — NO schema or API contract change (additive, downstream-only) [UPDATE v2.7.0]
  + [pre_build_knowledge] Object is selected from a governed inventory picker, never free-typed [UPDATE v2.8.0]
  + [pre_build_knowledge] Picker source = LP-05/LP-02 reads (inventory + attr counts); registration-actionable + Has-Updates are UI-derived
  + [pre_build_knowledge] FE/UX refinement only — no schema or API contract change (additive, downstream-only)
  + [pre_build_knowledge] Register captures the full attribute authoring contract; inline threshold is DB-driven [UPDATE v2.9.0]
  + [fixed_contracts] LP-05 object inventory read (picker source)
  + [fixed_contracts] LP-02 schema registry (schema drill-down)
  + [depends_on] LP-05
  + [depends_on] LP-30

Everything not listed above is unchanged from your v2.3.0 implementation. Leave it alone.

--- original background ---
A steward registers an object through a 5-step wizard. The object is chosen from a GOVERNED INVENTORY PICKER (no free-text object code): a Schema -> Object Type -> Object drill-down filtered to the registration-actionable subset (New / Has-Updates / Pending / Not-Eligible). The Register list defaults to a registration queue (actionable subset) with an 'All objects' toggle, synced to the full object inventory. Taxonomy/MDRM with jurisdiction is captured; submit lands the object DRAFT for governance approval.

## STEP 2 — Deliver this sub-task (React / TypeScript · shared)
Scope: [POINT-LEVEL CHANGE — sub-task scope is unchanged, but the parent point's scope/steps/tests moved. Re-verify this lane against the change list in the background.] React 18 + MUI + (AG Grid where tabular) components; Redux slice; calls service via API client; loading/error/empty states.
- If DECISION = REUSE: wire to the existing asset and write the conformance test proving the tests below pass.
- If DECISION = ADAPT: make the minimal extension/configuration to the asset you found. Do not fork it.
- If DECISION = BUILD: create new, following the branch's conventions.

## Tests must cover (>=90%; tests ship inside this sub-task)
- renders from API contract
- interactions match decided behavior
- loading/error/empty states
- coverage >=90%

## Pinned stack (use exactly; introduce nothing new)
- Backend: Java 17, Spring Boot 3.2.5, Spring Cloud Gateway; Controller->Service->DAO->Impl; NamedParameterJdbcTemplate (no JPA); SQL externalized in queries.properties via SQLQueryLoaderUtil; Jackson SNAKE_CASE
- Database: PostgreSQL 16, SQL-first, Flyway-owned DDL (no JPA); pgvector where AI
- Graph: Neo4j (Spring Data Neo4j); Cytoscape.js for graph visualization
- Frontend: React 18, MUI, Redux, AG Grid (Core standard); Vite build

## Fixed contract — build against this; the team OWNS executing it (apply via Flyway).
## Schema DESIGN changes flow upstream (downstream-only), never made unilaterally.
- LP-03 register API (fixed)
- LP-05 object inventory read (picker source)
- LP-02 schema registry (schema drill-down)
- TENANT theming

## Governance bars (non-waivable)
- Microservices only — never monolithic
- All authorization/business policy externalized to OPA — none in code
- >=90% unit-test coverage on new/changed code; tests live inside each logic point
- Self-healing on core path: retries, circuit-breaking, graceful degradation, health/recovery hooks [resilience library to confirm — P-07]
- Low-code: prefer reuse > configuration > extension point > new code
- Standards adherence: naming, datatypes, field lengths, must-have fields
- Configurable logo (TENANT.logoUrl) + client-pointable CSS (TENANT.cssOverrideUrl); multi-tenant theming
- Tenancy key = client_id; isolation OPA-enforced (no RLS day one; RLS = future option)
- Audit: metadata changes recorded to meta.metadata_change_history
- If a standard is absent, ASK the SME — do not assume
- Cross-cutting production-readiness is delivered as first-class logic points (Global Standards wave LP-34..LP-40), not only as bars: API docs, logging/diagnostics, datasource wiring, external config, global exception handling, coverage+completeness gate, app observability/resilience apply horizontally across every controller/service.
- Completeness gate (LP-39): generation fails when a required cross-cutting concern is absent, when an entity with a write has no GET/list read, or when a screen has no backing read API.

## Reference (behavior only — never copy prototype code): prototype + Simulation.html

## STEP 3 — At completion, emit an ADJUSTMENTS record (correction feedback)
If the developer/reviewer corrected anything, output an ADJUSTMENTS block so the manifest learns and
the next prompt is pre-corrected. For each correction give: SUMMARY (what), REASON (why), and CLASS:
- IMPLEMENTATION — a coding/wiring fix (e.g. "wire to existing TaxonomyService"). Auto-applies to the manifest.
- CONTRACT | STANDARD | DECISION — touches the data model, a standard, or a fixed decision. Do NOT apply it;
  raise it as a PROPOSAL to the owner (SME / Data Authority). The ring-fence holds: build corrections never
  silently rewrite the schema, standards, or a decision.

## Output
Lead with your STEP 1 findings (SEARCHED/FOUND/DECISION/CROSS-PRODUCT), then the
implementation or extension for this sub-task + its tests, then the STEP 3 ADJUSTMENTS block (or "ADJUSTMENTS: none").